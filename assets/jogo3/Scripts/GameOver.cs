using UnityEngine;
using System.Collections;
using UnityEngine.UI;

public class GameOver : MonoBehaviour {
    /*public Obstaculos [] destruirObstaculos;
    public brinquedo[] destruirBrinquedo;
    public racao_grande[] destruirRacaoGrande;
    public racao_pequena[] destruirRacaoPequena;
    public Pena[] destruirPena;
    public Ilustracoes[] destruirIlustracoes;*/

    // Use this for initialization
    void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
   /* public void Restart()
    {
        Time.timeScale = 1f;
        gameovermenu.SetActive(false);
        destruirObstaculos = FindObjectsOfType<Obstaculos>();
        for (int a =0; a < destruirObstaculos.Length; a++)
        {
            Destroy(destruirObstaculos[a]);
        }
        destruirBrinquedo = FindObjectsOfType<brinquedo>();
        for (int b = 0; b < destruirBrinquedo.Length; b++)
        {
            Destroy(destruirBrinquedo[b]);
        }
        destruirRacaoGrande = FindObjectsOfType<racao_grande>();
        for (int c = 0; c < destruirRacaoGrande.Length; c++)
        {
            Destroy(destruirRacaoGrande[c]);
        }
        destruirRacaoPequena = FindObjectsOfType<racao_pequena>();
        for (int d = 0; d < destruirRacaoPequena.Length; d++)
        {
            Destroy(destruirRacaoPequena[d]);
        }
        destruirPena = FindObjectsOfType<Pena>();
        for (int e = 0; e < destruirPena.Length; e++)
        {
            Destroy(destruirPena[e]);
        }
        destruirIlustracoes = FindObjectsOfType<Ilustracoes>();
        for (int f = 0; f < destruirIlustracoes.Length; f++)
        {
            Destroy(destruirIlustracoes[f]);
        }
        */

    }

