using UnityEngine;
using System.Collections;

public class Camira1 : MonoBehaviour {

    void Start()
    {
        

        Screen.orientation = ScreenOrientation.LandscapeLeft;
    }

    // Update is called once per frame
    void Update () {
	
	}
}
